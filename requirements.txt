pandas
numpy
scikit-learn
matplotlib
seaborn
tensorflow
keras
streamlit
joblib
jupyter
nbformat
nbconvert
# DistilBERT (feature extraction + optional fine-tuning).
# torch<2.3 is the last release with Intel macOS (x86_64) wheels; on every
# other platform (Windows, Linux, Apple Silicon) any recent torch works.
torch<2.3; sys_platform == "darwin" and platform_machine == "x86_64"
torch; sys_platform != "darwin" or platform_machine != "x86_64"
transformers>=4.40,<5
